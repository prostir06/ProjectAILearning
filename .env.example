# Скопіюйте в .env за потреби (файл .env у git не потрапляє).
FLASK_DEBUG=0
PORT=5000
HOST=127.0.0.1
FLASK_SECRET_KEY=change-me-in-production
# Обмеження рядків для python train.py --sample / subsample (0 = увесь CSV).
QUICK_TRAIN_MAX_ROWS=0
